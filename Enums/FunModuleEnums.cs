namespace VictorNovember.Enums;

public sealed class FunModuleEnums
{
    public enum CoinSide { Heads, Tails }
    public enum RpsChoice { Rock, Paper, Scissors }
    public enum RpsOutcome { Draw, PlayerWin, NovemberWin }
}
